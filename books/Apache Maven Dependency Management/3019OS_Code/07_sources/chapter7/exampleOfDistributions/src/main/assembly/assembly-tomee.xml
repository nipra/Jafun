<assembly xmlns="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.2"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.2 http://maven.apache.org/xsd/assembly-1.1.2.xsd">
    <id>distribution-tomee</id>

    <baseDirectory>${project.artifactId}-${project.version}</baseDirectory>
    <formats>
        <format>zip</format>
    </formats>
    <includeBaseDirectory>false</includeBaseDirectory>

    <dependencySets>
        <dependencySet>
            <outputDirectory>/</outputDirectory>
            <includes>
                <!-- Get the dependency-->
                <include>org.apache.openejb:tomee-webprofile:zip:1.5.2</include>
            </includes>
            <directoryMode>744</directoryMode>
            <!-- Uncompress the archive-->
            <unpack>true</unpack>
            <unpackOptions>
                <excludes>
                    <!-- exclude webapps folder-->
                    <exclude>**/webapps/**</exclude>
                </excludes>
            </unpackOptions>
        </dependencySet>
    </dependencySets>

    <fileSets>
        <fileSet>
            <directory>${project.build.directory}</directory>
            <outputDirectory>/apache-tomee-webprofile-1.5.2/webapps/</outputDirectory>
            <includes>
                <!--include the jar file generated by our project, then copy it into the folder hinted at in the 'outputDirectory' tag, just above-->
                <include>**/${project.artifactId}-${project.version}.${project.packaging}</include>
            </includes>
        </fileSet>
    </fileSets>
</assembly>